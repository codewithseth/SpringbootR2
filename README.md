# Spring Boot + Cloudflare R2 File Upload

```yaml
cloudflare:
  r2:
    endpoint: https://${ACCOUNT_ID}.r2.cloudflarestorage.com
    accessKey: ${ACCESS_KEY_ID}
    secretKey: ${SECRET_ACCESS_KEY}
    bucket: ${BUCKET_NAME}
```
